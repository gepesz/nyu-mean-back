extends layout
block content
    a(href="/things/new") Create a new thing
    h3
        | Current list:
    ul
        each thing in things
            li 
                a(href="/things/#{thing._id}")
                    =thing.name
                
    if things.length == 0
        .alert.alert-danger
            There are no things!